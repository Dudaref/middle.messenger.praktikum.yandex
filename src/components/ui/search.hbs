<input
  class="search {{classNames}}"
  type={{type}}
  id={{id}}
  name={{name}}
  placeholder={{placeholder}}
  onchange="this.value.length > 0 ? this.classList.add('isActive') : this.classList.remove('isActive')"
/>
